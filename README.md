###### tags : `Python`, `Azure`, `Flask-RESTful`, `NLP`, `MSSQL`, `GPT-2`

AIPen can generate candidate sentences and corresponding images based on the initial input sentence or paragraph provided by the user.

